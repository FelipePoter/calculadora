# Calculadora em C

Uma calculadora desenvolvida em C que permite fazer operações matemáticas no terminal.

## Como rodar

No terminal, navegue até o diretório onde está o `main.c` e execute:
```bash
gcc main.c -o main
```

Após compilar, execute o programa com:

```bash
./main
```
